# Server-Side API key from the FACEIT Developer Portal (App Studio -> Server-Side App).
# Required. Never commit your real key.
FACEIT_API_KEY=

# PORT is provided automatically by Render at runtime. Only set this locally
# if you want to run on a non-default port; otherwise the server defaults to 8000.
# PORT=8000
